{
  "name": "vulnlist",
  "version": "1.1.1",
  "type": "module",
  "bin": {
    "vulnlist": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "dist/"
  ],
  "scripts": {
    "build:dist": "gleam build && bun build --target bun ./entry.js --outfile=dist/vulnlist.js"
  }
}
